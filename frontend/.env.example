# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local for Next.js to use it
# NEVER commit .env.local to git (it's in .gitignore)

# Backend API URL
# Local development: http://localhost:3001/api
# Production: https://your-api-domain.com/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Contract Addresses
# IMPORTANT: Update these after deploying contracts
# Get these addresses from the deployment script output in contracts/
# These must match the addresses in backend/.env
# Format: 0x followed by 40 hex characters
NEXT_PUBLIC_COLLATERAL_LOCK_ADDRESS=0x0000000000000000000000000000000000000000
NEXT_PUBLIC_VERIFICATION_NFT_ADDRESS=0x0000000000000000000000000000000000000000

# Wallet Connect Project ID (Optional)
# Only needed if you want WalletConnect support (mobile wallets)
# Get from https://cloud.walletconnect.com
# If not provided, app will use injected wallets (MetaMask, etc.)
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=
